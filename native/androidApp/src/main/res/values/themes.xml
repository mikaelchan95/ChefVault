<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base window theme; Compose (ChefVaultTheme) handles all content theming. -->
    <style name="Theme.ChefVault" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
